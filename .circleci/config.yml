version: 2.0

jobs:
  build:
    docker:
      - image: armageddonknight/texlive:latest
    steps:
      - checkout
      - run: mkdir -p /tmp/artifacts/ && make all && mv main.pdf /tmp/artifacts/
      - store_artifacts:
          path: /tmp/artifacts
